- v5 :

  * Update Debian package
  * Create a PPA for ubuntu
je vais d
- v6 :
  * Better cover graphics for movies (add reflection and shadow)
  * libcurl is used in url_utils. This part seems to do actually the same as ecore_con_url so remove this old code and use ecore_con instead. 
  * Remove libvalhalla definitively and use our own parser based on EMS
  * Read video from the beginning or since the last known position
  * Mark video as read
  * E_Udisk for disk detection.
  * cdda/dvd plugin
  * Smooth transition when go back to mainmenu after end of video
  * Transition when browsing

- v7 :
  * Add integration with transmission.
